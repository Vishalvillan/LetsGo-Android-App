<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.example.letsgo">

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity android:name=".after"></activity>
        <activity android:name=".all2" />
        <activity android:name=".work" />
        <activity android:name=".walking" />
        <activity android:name=".textmove" />
        <activity android:name=".watch" />
        <activity android:name=".workPlan" />
        <activity android:name=".plan" />
        <activity android:name=".videos" />
        <activity
            android:name=".Every"
            android:label="@string/title_activity_every"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".all" />
        <activity android:name=".diet" />
        <activity android:name=".inside" />
        <activity android:name=".signup" />
        <activity android:name=".signin" />
        <activity android:name=".MainActivity">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
    </application>

</manifest>